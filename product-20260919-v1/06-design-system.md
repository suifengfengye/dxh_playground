# 工作 4 第一步：设计系统（待确认）

版本：v1 / 2026-09-19。性质：视觉方向与 Token 提案，**尚未在任何真实页面验证**。
输入来源：`ui-ux-pro-max` 本地知识库检索、`design-system` 技能的三层 Token 架构、以及 03/05 已确认的交互结论。
交付载体（用户已选）：静态高保真 HTML 稿。本文档确认后才开始出稿。

## 1. 一句话方向

**安静的文档工作台：中性纸白画布 + 墨色为主 + 只把一种识别色留给 AI。**

按钮用墨黑，不用品牌色；唯一的高饱和识别色（青）专门标记"这是 AI 说的"。这样用户在任何一屏都能一眼分清 **AI 提议 / 我确认过的 / 系统事实** —— 这正是 05 与 02 §6.5 反复强调的信任边界。

## 2. 知识库给了什么，我否决了什么

必须说明：检索返回的是一套通用"Productivity Tool"配置，其中三处与我们的产品冲突，不能照抄。

| 返回项 | 处理 | 原因 |
|---|---|---|
| 模式 `Product Demo + Features`（Hero > 视频 > 卖点 > CTA） | **否决** | 那是营销落地页的漏斗结构。我们的对象是应用内工作台，不是获客页。与 v1 否决"营销漏斗"一致 |
| 背景色 `#F0FDFA`（浅青底） | **否决** | 这是文档工具。用户要判断简历在纸上的效果，带色背景会干扰对"纸白"的感知；大面积浅青还会把界面推向健康/水疗语域 |
| 强调色 `#EA580C`（橙）作 CTA | **否决** | 响亮橙色与"安静、专业"冲突，且与青同时出现会抢注意力。橙色改作**语义色**：只表示"需要注意" |
| 风格 `Flat Design` 中的 "bold colors / icon-heavy" | **部分否决** | 采纳"扁平、无渐变、少装饰"，不采纳高饱和与图标堆砌 |
| 字体 `Plus Jakarta Sans` 为唯一字体 | **降为备选** | 圆润友好的气质偏消费级；文档工作台更需要长文可读性与中立感 |
| 主色 `#0D9488` 直接当正文/链接色 | **否决该用法** | 对白底对比度约 **3.7:1**，达不到正文 4.5:1 的 AA 标准。文字改用 `#0F766E`（约 5.0:1），原色只用于大图形与图标 |

**采纳的部分**：扁平淡雅的方向、无渐变、简单 hover、150–200ms 过渡、少而一致的图标；以及它的无障碍清单（对比度、可见焦点、reduced motion、禁用 emoji 当图标、375/768/1024/1440 断点）。另外它列出要避免的 `Complex onboarding`，与我们刚砍掉三步表单的方向一致，可作为旁证。

## 3. 色彩系统

三层结构：Primitive（原始值）→ Semantic（用途）→ Component（组件）。组件里不写十六进制。

### 3.1 Primitive

```css
--neutral-50:  #FAFAF9;  /* 暖中性，纸感 */
--neutral-100: #F5F5F4;
--neutral-200: #E7E5E4;
--neutral-300: #D6D3D1;
--neutral-600: #57534E;
--neutral-900: #1C1917;

--teal-600: #0D9488;   /* 大图形可用 */
--teal-700: #0F766E;   /* 文字/链接可用 */
--teal-50:  #F0FDFA;   /* 仅小块底 */
--teal-200: #99F6E4;

--amber-700: #B45309;
--green-700: #15803D;
--red-600:   #DC2626;
```

### 3.2 Semantic

| Token | 值 | 用途 | 对白底对比度 |
|---|---|---|---|
| `--color-canvas` | `#FAFAF9` | 应用背景 | — |
| `--color-surface` | `#FFFFFF` | 卡片、文档纸面 | — |
| `--color-border` | `#E7E5E4` | 常规分隔 | — |
| `--color-border-strong` | `#D6D3D1` | 表格头、需强调边界 | — |
| `--color-text` | `#1C1917` | 正文与标题 | ≈17:1 |
| `--color-text-muted` | `#57534E` | 辅助说明、时间戳 | ≈7.6:1 |
| `--color-primary` | `#1C1917` | 主按钮底 | — |
| `--color-on-primary` | `#FFFFFF` | 主按钮文字 | ≈17:1 |
| `--color-attention` | `#B45309` | 需复核、临近截止 | ≈5.0:1 |
| `--color-success` | `#15803D` | 检查通过、已确认 | ≈5.0:1 |
| `--color-danger` | `#DC2626` | 破坏性操作、检查失败 | ≈4.8:1 |
| `--color-focus-ring` | `#0F766E` | 键盘焦点环 | ≈5.0:1 |

对比度为按 WCAG 相对亮度公式手工计算，**实现阶段必须用工具复算**（真实字号/字重会改变结论）。

### 3.3 AI 语义层（本产品的特殊需求）

草图里反复出现"AI 提议 vs 用户确认"，这需要在 Token 层就固定，而不是每个页面各画各的。

| Token | 值 | 用途 |
|---|---|---|
| `--ai-surface` | `#F0FDFA` | AI 结论卡、改动日志底色（小面积） |
| `--ai-border` | `#99F6E4` | AI 卡片边框 |
| `--ai-text` | `#0F766E` | AI 相关文字 |
| `--ai-mark` | `#0D9488` | 左侧色条、图标 |

**强制规则**：AI 内容不能只靠颜色区分。每个 AI 区块必须同时满足三条 —— 固定标签文字（`Assistant`）、左侧 3px 色条、浅底。色盲用户与灰度打印下仍然可辨。

## 4. 字体系统

### 4.1 三套字体必须分开，不能混为一谈

| 用途 | 字体 | 理由 |
|---|---|---|
| 产品 UI（标题、按钮、标签、表格） | **Lexend** | 为阅读能力设计的字体，可读性与无障碍表现优于圆润型字体 |
| 长文阅读（职位原文、说明文字） | **Source Sans 3** | 正文阅读舒适，字重齐全 |
| **简历 PDF 模板** | **本轮不选** | 这是交付给雇主和 ATS 的文件，不是产品界面。必须单独按 ATS 可解析性选型，与 UI 字体解耦 |

第三行是最容易出错的地方：把界面字体塞进简历模板，可能让 PDF 在 ATS 解析或雇主观感上出问题。留到模板设计阶段单独决策。

### 4.2 字阶与排版

```css
--font-ui:      'Lexend', system-ui, sans-serif;
--font-reading: 'Source Sans 3', system-ui, sans-serif;

--text-caption: 12px;  /* 时间戳、辅助说明 */
--text-sm:      13px;  /* 表格、密集列表 */
--text-body:    15px;  /* 默认正文 */
--text-lg:      18px;  /* 区块标题 */
--text-h3:      20px;
--text-h2:      24px;
--text-h1:      30px;

--leading-tight: 1.25;  /* 标题 */
--leading-body:  1.55;  /* 正文 */
--measure:       68ch;  /* 长文最大行宽 */
```

规则：日期与计数用 tabular figures（等宽数字），否则表格里的日期会跳动；长文不超过 `--measure`，避免宽屏上一行读到底。

## 5. 间距、圆角、层级、动效

```css
--space-1: 4px;  --space-2: 8px;   --space-3: 12px;  --space-4: 16px;
--space-5: 20px; --space-6: 24px;  --space-8: 32px;  --space-12: 48px;

--radius-sm: 4px;   /* 输入框、chip */
--radius-md: 6px;   /* 按钮、卡片 */
--radius-lg: 10px;  /* 对话框、面板 */

--motion-fast: 120ms;  --motion-base: 180ms;
--ease-out: cubic-bezier(0.16, 1, 0.3, 1);
```

**层级策略：用边框分层，而不是阴影。** 只有对话框与下拉浮层允许一层轻阴影。原因是这个产品是高密度列表 + 文档预览，多层阴影会让界面发脏，也让"纸面"失去平面感。

动效：只用于状态切换与浮层进出；`prefers-reduced-motion` 下关闭非必要动效；禁止做数字滚动之类的装饰动画。

## 6. 状态色：把状态和颜色解耦

状态很多，如果每个状态一个颜色会变成彩虹，反而看不懂。规则如下。

| 状态组 | 表现 | 是否用色 |
|---|---|---|
| 业务状态（Saved / Drafting / Ready / Applied / Interview / Offer / Closed） | 中性 chip + 文字；仅 Ready 与 Offer 用 success | 少量 |
| 事实状态（Needs confirmation / Confirmed / Optional / Missing） | attention / success / 中性 / 中性 | 是 |
| 检查结果（Pass / Fail / Not stated / Reviewed by you） | success / danger / 中性 / 墨色+对勾 | 是 |
| 结构化结果不可用（S12） | **中性**，不是红色 | 否 |

`Not stated` 与"拿不到结构化结果"都必须显示为中性，**不能显示 0、不能用红色** —— 它们不是失败，是未知。这条直接对应 02 的 A15。

## 7. 组件清单与关键规格

树形列出需要在静态高保真稿里全部画到的组件。

| 组件 | 关键规格 |
|---|---|
| 顶栏 / 侧栏 / 内容区 / 右栏 rail | 侧栏固定 2 项导航；右栏在窄屏折叠为抽屉 |
| 主按钮 / 次按钮 / 幽灵按钮 / 破坏性按钮 | 主按钮墨底白字；hover 变 `#292524`；**AI 动作按钮单独一档**（浅青底 + 青边） |
| AI 结论卡（Assistant） | `--ai-surface` 底 + 3px 左色条 + `Assistant` 标签，见 §3.3 |
| 事实卡片 | 标题行 + 状态 chip + 内容 + `Looks right / Edit / Remove`；Edit 就地展开为带可见标签的字段 |
| 清单 sidebar（Profile checklist） | 四组具名条目；`[x] / [ ]`；**不使用百分比进度条** |
| 对话流 + composer | 用户气泡与 AI 卡片视觉不同；追问建议以 chip 出现，不生成新表单 |
| 建议条（Assistant strip） | 位于 P03 顶部，最多两条建议 + 对应按钮 |
| 列表 / 表格 | 表头 `--neutral-100`；行分隔用 1px 边框；状态 chip 与"下一步"对齐；`Assistant read` 列允许多行 |
| 标签页 | 底部 2px 墨色下划线表示选中，不用背景色块 |
| 文档编辑区 + 段落选中 | 纸面 `--color-surface` + 1px 边框；选中段落用 1px 虚线圈 |
| 证据/理由面板、改动日志 | 归入 AI 语义层；每条改动可 `Keep / Restore` |
| 检查结果列表 | 前缀文字 `[Pass] / [Fail] / Not stated`，不只用图形符号 |
| 状态 chip | 高度 20–22px，`--radius-sm`，1px 边框，12px 字 |
| 对话框（D01–D03） | `--radius-lg`，唯一使用阴影的组件；有遮罩；焦点管理 |
| 空态 / 错误态 / 降级态 | 每种都必须有：一句说明 + 保留了什么 + 一个下一步动作 |
| 上传 / 粘贴输入 | 三个入口同级；拖拽区用虚线边框，不用大面积插画 |

图标：统一用 **Lucide**（与 shadcn/ui 生态一致），线性、1.5px 描边，尺寸 Token 化（16 / 20 / 24）。**不用 emoji 当图标。**

## 8. 无障碍底线

- 正文对比度 ≥ 4.5:1；图形与边框 ≥ 3:1；焦点环可见且不被遮挡。
- 任何状态都不能只靠颜色：必须"文字 + 图标 + 边框"三重编码。
- 键盘可完成全部核心路径：登录 → 录入 → 导入职位 → 看适配 → 编辑 → 导出 → 更新状态。
- 表单字段始终有可见标签（对话式录入也一样），错误在顶部汇总并链接到字段。
- `prefers-reduced-motion` 生效；长任务进度由真实状态驱动，不伪造百分比。
- 断点：375 / 768 / 1024 / 1440。

## 9. 已确认的决策（2026-09-19）

用户已确认，以下为定稿，不再作为待议项：

1. **主 CTA 用墨黑（`#1C1917`），青色只留给 AI 语义层。** 接受"按钮不是品牌色"这一代价，换取一屏内可分辨 AI 提议 / 用户确认 / 系统事实。
2. **界面字体采用 Lexend + Source Sans 3。** 可读性优先于圆润友好；不采用 Plus Jakarta Sans。
3. **首版只交付 light mode。** dark 仅保留语义层 Token 位置，不承诺可交付；简历 PDF 模板字体仍留待模板阶段单独决策。

## 10. 下一步

本文档确认后，按此 Token 出**静态高保真 HTML 稿**。建议顺序：先做 P02（录入，交互最复杂、最能暴露字体与 AI 语义层的问题）与 P05（适配，信息密度最高），这两页立住了，其余页面基本是套用。

本轮**不**做：简历 PDF 模板字体选型、暗色模式配色、动效编排、组件库选型落地。
